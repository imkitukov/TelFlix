@model IEnumerable<SelectListItem>

@{
    ViewData["Title"] = "Add To Role";
}

<h2>Add User To Role</h2>

<form method="post">
    <select asp-items="Model" name="role" class="form-control"></select>
    <button type="submit" class="btn btn-primary">Save Role</button>
</form>
